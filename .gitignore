# --- dependencies ---
node_modules/
.pnp/
.pnp.*

# --- secrets & env — NEVER commit ---
.env
.env.*
!.env.example
*.key
*.pem
# résumé signing private key — human-only, never committed
apps/web/.keys/
*.crt
*.p12
secrets/
*.secret
# k8s Secret manifests — never commit a real one; the *.example.yaml shape files are allowed
*.secret.yaml
!*.secret.example.yaml
*_rsa
*_ed25519
kubeconfig
*.kubeconfig

# --- build output ---
dist/
build/
out/
.next/
.turbo/
# Rust/Go build artifacts where applicable
target/
# TypeScript incremental build cache
*.tsbuildinfo

# --- logs / temp / caches ---
*.log
.cache/
tmp/
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# --- research scratch: third-party site teardown (do not redistribute) ---
.research/kaveenk/
.research/activetheory/

# (kept on purpose: .research/identity/ — our own brand assets/renders)

# secrets — never commit
*.env
**/gipc.json
/etc/cloudflared/

# --- syncthing (Mac↔box sync artifacts) ---
.syncthing.*
.stversions/
.stfolder

# --- python (services/ai) ---
.venv/
venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/

# --- terraform (infra/terraform) — state/creds never committed ---
**/.terraform/*
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
crash.log
.terraform.lock.hcl

# Private career-tooling workspace — dossier + PII, never in the repo
career/
