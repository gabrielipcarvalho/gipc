apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# A base — referenced by infra/k8s/core/kustomization.yaml via `resources: [../caddy]`, so one
# gipc-core ArgoCD app manages core + caddy. Caddy's image is pinned inline in caddy.yaml (no images: block).
resources:
  - caddy.yaml
