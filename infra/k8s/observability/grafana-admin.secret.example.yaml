# EXAMPLE ONLY — the shape of the grafana-admin Secret. Never commit a real password.
# The REAL Secret is created imperatively on garuda (value lives only in the cluster):
#   pw=$(openssl rand -base64 24)
#   kubectl -n observability create secret generic grafana-admin \
#     --from-literal=admin-password="$pw" --dry-run=client -o yaml | kubectl apply -f -
# This file is NOT in kustomization.yaml (never applied).
apiVersion: v1
kind: Secret
metadata:
  name: grafana-admin
  namespace: observability
type: Opaque
stringData:
  admin-password: CHANGEME
