apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: gipc
  labels: { app: web }
spec:
  type: NodePort
  selector: { app: web }
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30081   # parallel to holding (30080); tunnel repointed to 30081 after web verified
