apiVersion: v1
kind: Service
metadata:
  name: web
  namespace: gipc
  labels: { app: web }
spec:
  type: NodePort
  selector: { app: web }
  ports:
    - port: 80
      targetPort: 3000
      nodePort: 30080   # cloudflared tunnel already points at localhost:30080
