# CI pins the built image tag below ([skip ci] commit). Keep comments free of the literal
# tag-key token so the sed can't match them (core's lesson).
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: gipc
resources:
  - ai.yaml
  - ollama.yaml
images:
  - name: ghcr.io/gabrielipcarvalho/gipc-ai
    newTag: "fcf83bf5ea254aff1937b33610c569d548f41fae"
