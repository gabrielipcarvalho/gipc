# SHAPES ONLY — NOT in kustomization.yaml (never applied). The real Secrets are imperative on garuda:
#   ai-secrets:  PW=$(kubectl -n data get secret postgres -o jsonpath='{.data.password}' | base64 -d)
#                kubectl -n gipc create secret generic ai-secrets \
#                  --from-literal=database-url="postgresql://gipc:${PW}@postgres.data.svc:5432/gipc_ai"
#   turnstile:   kubectl -n gipc create secret generic turnstile --from-literal=secret=<test-or-real-key>
#   anthropic:   kubectl -n gipc create secret generic anthropic --from-literal=api-key=<key>  (credential wall)
apiVersion: v1
kind: Secret
metadata:
  name: ai-secrets
  namespace: gipc
type: Opaque
stringData:
  database-url: CHANGEME
---
apiVersion: v1
kind: Secret
metadata:
  name: turnstile
  namespace: gipc
type: Opaque
stringData:
  secret: CHANGEME
---
apiVersion: v1
kind: Secret
metadata:
  name: anthropic
  namespace: gipc
type: Opaque
stringData:
  api-key: CHANGEME
